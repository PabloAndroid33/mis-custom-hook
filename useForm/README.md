#UseForm


Ejemplo:
````
        const initialForm={
            name:'',
            age=0,
            email=''
        }
        const [value,handleInputChange,resetInput]=useForm(initialForm);

````